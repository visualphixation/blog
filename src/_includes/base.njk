<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ '/css/style.css' | url }}"/>
    <title>{{ title }}</title>
  </head>
  <body>
    <header>
      {% include 'skip-nav.njk' %}
      <div class="logo">
        {% include 'logo.njk' %}
      </div>
      {% include 'navigation.njk' %}
    </header>
    <main id="main-content">
      <h1>{{ title }}</h1>
      {{ content | safe }}
    </main>
    <aside aria-label="related content section" class="content-related">
      {% include 'random-post.njk' %}
    </aside>
    {% include 'footer.njk' %}
  </body>
</html>